---
interface Props {
  title: string;
  description?: string;
  image?: string;
  url?: string;
}

const {
  title,
  description = "注目のインディーゲームを関西弁で紹介するサイト",
  image = "/og-image.png",
  url = Astro.url.href
} = Astro.props;
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | Indie Game Discover</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={url} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- OGP Tags -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Indie Game Discover" />
    <meta property="og:title" content={`${title} | Indie Game Discover`} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={image} />
    <meta property="og:url" content={url} />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={`${title} | Indie Game Discover`} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={image} />

    <!-- Google Fonts (Optimized) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Noto+Sans+JP:wght@400;500;700&display=swap" onload="this.rel='stylesheet'" />
    <noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Noto+Sans+JP:wght@400;500;700&display=swap" /></noscript>
    <style>
      :root {
        /* 既存のCSS変数（維持） */
        --bg: #0f0f23;
        --text: #e0e0e0;
        --accent: #7c3aed;
        --card-bg: #1a1a2e;
        --border: #2a2a4a;

        /* カラー拡張 */
        --accent-glow: rgba(124, 58, 237, 0.5);
        --accent-green: #10b981;
        --gradient: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #0f0f23 100%);
        --text-muted: #a1a1b5; /* WCAG AA基準対応（コントラスト比4.5:1以上） */

        /* タイポグラフィスケール */
        --font-xs: 0.75rem;    /* 12px */
        --font-sm: 0.875rem;   /* 14px */
        --font-base: 1rem;     /* 16px */
        --font-lg: 1.125rem;   /* 18px */
        --font-xl: 1.25rem;    /* 20px */
        --font-2xl: 1.5rem;    /* 24px */
        --font-3xl: 1.875rem;  /* 30px */
        --font-4xl: 2.25rem;   /* 36px */

        /* 8pxグリッドスペーシング */
        --space-1: 0.25rem;  /* 4px */
        --space-2: 0.5rem;   /* 8px */
        --space-3: 0.75rem;  /* 12px */
        --space-4: 1rem;     /* 16px */
        --space-5: 1.25rem;  /* 20px */
        --space-6: 1.5rem;   /* 24px */
        --space-8: 2rem;     /* 32px */
        --space-10: 2.5rem;  /* 40px */
        --space-12: 3rem;    /* 48px */
        --space-16: 4rem;    /* 64px */

        /* エレベーション/シャドウシステム */
        --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.2);
        --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.3);
        --shadow-lg: 0 12px 24px rgba(0, 0, 0, 0.4);
        --shadow-xl: 0 20px 40px rgba(0, 0, 0, 0.5);
        --shadow-glow: 0 0 20px rgba(124, 58, 237, 0.3);
      }

      /* グローバルスタイル */
      html {
        scroll-behavior: smooth;
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }

      body {
        font-family: 'Inter', 'Noto Sans JP', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: var(--gradient);
        color: var(--text);
        line-height: 1.7;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      /* カスタムスクロールバー */
      body::-webkit-scrollbar {
        width: 12px;
      }
      body::-webkit-scrollbar-track {
        background: var(--bg);
      }
      body::-webkit-scrollbar-thumb {
        background: var(--accent);
        border-radius: 6px;
      }
      body::-webkit-scrollbar-thumb:hover {
        background: var(--accent-glow);
      }

      /* 選択時のハイライト */
      ::selection {
        background: var(--accent);
        color: white;
      }

      /* キーボードフォーカスリング */
      :focus-visible {
        outline: 2px solid var(--accent);
        outline-offset: 2px;
        border-radius: 4px;
      }
      /* マウスクリック時はフォーカスリング非表示 */
      :focus:not(:focus-visible) {
        outline: none;
      }

      /* スキップナビゲーション */
      .skip-link {
        position: absolute;
        top: -100px;
        left: 0;
        background: var(--accent);
        color: white;
        padding: var(--space-2) var(--space-4);
        text-decoration: none;
        font-weight: 600;
        z-index: 9999;
        border-radius: 0 0 4px 0;
      }
      .skip-link:focus {
        top: 0;
      }

      /* Header */
      header {
        position: sticky;
        top: 0;
        z-index: 100;
        background: rgba(15, 15, 35, 0.8);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid var(--border);
        padding: var(--space-4) var(--space-8);
      }

      header nav {
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      header .logo {
        font-size: var(--font-2xl);
        font-weight: 700;
        background: linear-gradient(90deg, var(--accent) 0%, var(--accent-green) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: var(--space-2);
      }

      header .nav-links {
        display: flex;
        gap: var(--space-6);
      }

      header .nav-links a {
        color: var(--text);
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s ease;
      }

      header .nav-links a:hover {
        color: var(--accent);
      }

      /* Main */
      main {
        max-width: 1200px;
        margin: var(--space-8) auto;
        padding: 0 var(--space-8);
        flex: 1;
      }

      /* Footer */
      footer {
        border-top: 1px solid var(--border);
        padding: var(--space-8);
        text-align: center;
        background: rgba(15, 15, 35, 0.5);
      }

      footer .footer-content {
        max-width: 1200px;
        margin: 0 auto;
      }

      footer .footer-links {
        display: flex;
        justify-content: center;
        gap: var(--space-8);
        margin-bottom: var(--space-4);
      }

      footer .footer-links a {
        color: var(--accent);
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s ease;
      }

      footer .footer-links a:hover {
        color: var(--accent-green);
      }

      footer .footer-description {
        color: var(--text-muted);
        font-size: var(--font-sm);
      }

      /* 共通スタイル */
      img { max-width: 100%; border-radius: 8px; }

      /* テーブルスタイル（リッチデザイン） */
      table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0;
        margin: var(--space-6) 0;
        background: var(--card-bg);
        border-radius: 12px;
        overflow: hidden;
        box-shadow: var(--shadow-md), var(--shadow-glow);
        backdrop-filter: blur(10px);
      }

      th, td {
        padding: var(--space-4) var(--space-5);
        text-align: left;
        border-bottom: 1px solid var(--border);
        transition: all 0.3s ease;
      }

      /* ヘッダー行 */
      th {
        background: linear-gradient(135deg, var(--card-bg) 0%, rgba(124, 58, 237, 0.1) 100%);
        font-weight: 600;
        color: var(--accent);
        text-transform: uppercase;
        font-size: var(--font-sm);
        letter-spacing: 0.05em;
        border-left: 3px solid var(--accent);
      }

      th:first-child {
        border-left: none;
        padding-left: var(--space-6);
        border-left: 4px solid var(--accent);
      }

      /* データ行 */
      tbody tr {
        transition: all 0.3s ease;
      }

      tbody tr:nth-child(even) {
        background: rgba(255, 255, 255, 0.02);
      }

      tbody tr:nth-child(odd) {
        background: rgba(0, 0, 0, 0.1);
      }

      /* ホバーエフェクト */
      tbody tr:hover {
        background: rgba(124, 58, 237, 0.15);
        transform: translateX(4px);
      }

      tbody tr:hover td {
        color: #fff;
      }

      /* 項目セル（最初の列）にアクセントボーダー */
      td:first-child {
        font-weight: 600;
        color: var(--accent-green);
        border-left: 4px solid var(--accent);
        padding-left: var(--space-6);
        background: rgba(124, 58, 237, 0.05);
      }

      /* 最終行のボーダー削除 */
      tbody tr:last-child td {
        border-bottom: none;
      }

      /* レスポンシブ対応（モバイル） */
      @media (max-width: 768px) {
        table {
          border-radius: 8px;
          font-size: var(--font-sm);
        }

        th, td {
          padding: var(--space-3) var(--space-4);
        }

        th:first-child,
        td:first-child {
          padding-left: var(--space-4);
        }

        /* モバイルでは横スクロール可能に */
        main {
          overflow-x: auto;
        }
      }

      @media (max-width: 480px) {
        table {
          font-size: var(--font-sm);
        }

        th, td {
          padding: var(--space-2) var(--space-3);
        }
      }

      blockquote {
        border-left: 3px solid var(--accent);
        padding: var(--space-2) var(--space-4);
        margin: var(--space-4) 0;
        background: var(--card-bg);
        border-radius: 0 8px 8px 0;
      }
      a { color: var(--accent); }
      h1, h2 { margin: var(--space-6) 0 var(--space-2); }
      hr { border-color: var(--border); margin: var(--space-8) 0; }

      /* 購入リンクセクション */
      /* hrの後のpタグ内のSteamリンクをボタン風に */
      hr + p {
        background: var(--card-bg);
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: var(--space-6);
        margin: var(--space-8) 0;
        box-shadow: var(--shadow-md);
        display: flex;
        flex-direction: column;
        gap: var(--space-4);
      }

      /* Steamストアリンクをボタン風に */
      a[href*="store.steampowered.com"] {
        display: inline-flex;
        align-items: center;
        gap: var(--space-3);
        padding: var(--space-4) var(--space-6);
        border-radius: 8px;
        font-weight: 600;
        font-size: var(--font-base);
        text-decoration: none;
        transition: all 0.3s ease;
        box-shadow: var(--shadow-sm);
        background: linear-gradient(135deg, #1b2838 0%, #2a475e 100%);
        color: #66c0f4;
        border: 2px solid #66c0f4;
        position: relative;
        overflow: hidden;
      }

      a[href*="store.steampowered.com"]::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
        transition: left 0.5s ease;
      }

      a[href*="store.steampowered.com"]:hover::before {
        left: 100%;
      }

      a[href*="store.steampowered.com"]:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-md);
        background: linear-gradient(135deg, #2a475e 0%, #1b2838 100%);
        color: white;
      }

      /* アフィリエイト注記 */
      hr + p::after {
        content: '※ 購入リンクにはアフィリエイトが含まれる場合があります';
        display: block;
        margin-top: var(--space-4);
        padding-top: var(--space-4);
        border-top: 1px solid var(--border);
        font-size: var(--font-xs);
        color: var(--text-muted);
        text-align: center;
      }

      /* モバイル対応 */
      @media (max-width: 768px) {
        hr + p {
          padding: var(--space-5);
        }

        a[href*="store.steampowered.com"] {
          width: 100%;
          justify-content: center;
        }
      }

      @media (max-width: 480px) {
        hr + p {
          padding: var(--space-4);
        }

        a[href*="store.steampowered.com"] {
          padding: var(--space-3) var(--space-5);
          font-size: var(--font-sm);
        }
      }
    </style>
  </head>
  <body>
    <a href="#main" class="skip-link">メインコンテンツへ</a>
    <header>
      <nav aria-label="メインナビゲーション">
        <a href="/" class="logo">🎮 Indie Game Discover</a>
        <div class="nav-links">
          <a href="/">ホーム</a>
        </div>
      </nav>
    </header>
    <main id="main">
      <slot />
    </main>
    <footer>
      <div class="footer-content">
        <div class="footer-links">
          <a href="https://store.steampowered.com/" target="_blank" rel="noopener noreferrer" aria-label="Steam公式サイト（外部サイト）">Steam</a>
          <a href="https://github.com" target="_blank" rel="noopener noreferrer" aria-label="GitHub（外部サイト）">GitHub</a>
        </div>
        <p class="footer-description">注目のインディーゲームを発掘・紹介するサイト</p>
      </div>
    </footer>
    <!-- Cloudflare Web Analytics -->
    <!-- トークンはCloudflareダッシュボードで取得後に設定 -->
    <!-- <script defer src='https://static.cloudflareinsights.com/beacon.min.js' data-cf-beacon='{"token": "YOUR_TOKEN"}'></script> -->
    <!-- End Cloudflare Web Analytics -->
  </body>
</html>
